package com.foo.homework;

// Node hold an integer in a binary tree.
class Node {
    final int key;
    Node left, right;

    public Node(int data) {
        key = data;
        left = right = null;
    }
}
